{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target",
      "args": [],
      "cwd": "${workspaceFolder}",
      "internalConsoleOptions": "openOnSessionStart",
      "preLaunchTask": "build"
    }
  ]
}
